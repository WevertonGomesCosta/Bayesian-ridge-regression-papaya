---
title: "Derivação dos resultados e seleção dos modelos"
author:
  - Costa, W. G.^[Weverton Gomes da Costa, Pós-Doutorando, Departamento de Estatística - Universidade Federal de Viçosa, weverton.costa@ufv.br]
date: "`r Sys.Date()`"
site: workflowr::wflow_site
url: https://wevertongomescosta.github.io/Bayesian-ridge-regression-papaya/
bibliography: ../REFERENCES.bib
link-citations: true
output:
  workflowr::wflow_html:
    toc: true
    toc_depth: 3
    toc_float: true
    code_download: true
    code_folding: show
editor_options:
  chunk_output_type: console
github-repo: WevertonGomesCosta/Bayesian-ridge-regression-papaya
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  warning = FALSE,
  message = FALSE,
  cache = FALSE,
  fig.align = "center",
  out.width = "100%"
)
```

## 1. Objetivo

A partir das predições, métricas e diagnósticos obtidos no Módulo 03, este
módulo compara os sete métodos dentro de cada característica e deriva os
resultados utilizados na seleção genômica.

São calculados o desempenho preditivo OOF, DIC e medidas relativas de suporte,
a decisão do método por característica, a estabilidade entre folds, o tempo
computacional, a herdabilidade e a acurácia no análogo RR-BLUP/BRR, os valores
genômicos OOF, os rankings orientados, a seleção proporcional, a recorrência
entre características e as correlações entre valores genômicos.

A direção de melhoramento é definida antes do ranking: aumentar MMCOM, PRODCOM,
CF, DF, EP, FFC, FFP e SS; diminuir PRODEF e DCI.

## 2. Pacotes e resultados do ajuste dos modelos

```{r packages}
suppressPackageStartupMessages({
  library(dplyr)
  library(tidyr)
  library(knitr)
  library(here)
})
```

A etapa seguinte carrega os objetos de entrada e organiza as estruturas necessárias para a análise.

```{r inputs}
MATRICES_FILE <- here("output", "matrices", "matrices_object.rds")
MODELS_FILE <- here("output", "models", "models_object.rds")
RESULTS_DIR <- here("output", "results", "m04")

dir.create(RESULTS_DIR, recursive = TRUE, showWarnings = FALSE)

matrices_object <- readRDS(MATRICES_FILE)
models_object <- readRDS(MODELS_FILE)

metrics <- models_object$metrics_by_fold
predictions <- models_object$predictions
convergence <- models_object$convergence_diagnostics

traits <- colnames(matrices_object$phenotypes)
methods <- models_object$settings$methods
```

As dimensões das três tabelas recebidas mostram a cobertura da análise antes
das derivações seguintes.

```{r input-summary}
# Monta a tabela-resumo apresentada nesta seção.
data.frame(
  Saida = c(
    "Métricas característica × fold × método",
    "Predições OOF",
    "Diagnósticos MCMC"
  ),
  Linhas = c(
    nrow(metrics),
    nrow(predictions),
    nrow(convergence)
  ),
  # Registra os valores de `Caracteristicas` que completam esta estrutura.
  Caracteristicas = c(
    n_distinct(metrics$Trait),
    n_distinct(predictions$Trait),
    n_distinct(convergence$Trait)
  ),
  Metodos = c(
    n_distinct(metrics$Method),
    n_distinct(predictions$Method),
    n_distinct(convergence$Method)
  )
) |>
  kable(caption = "Resultados recebidos do ajuste dos modelos")
```

## 3. Desempenho preditivo OOF

As estimativas OOF herdam o desenho estratificado por família dos Módulos 02
e 03. Elas quantificam a predição dentro da população de melhoramento
representada e não estimam a generalização para famílias inteiramente novas.

As métricas principais são recalculadas com as 150 predições fora da amostra
de cada combinação característica × método. Cada indivíduo contribui uma única
predição OOF para cada método.

```{r pooled-oof-metrics}
# Combina as predições OOF e calcula as métricas preditivas agregadas por característica e método.
pooled_metrics <- predictions |>
  group_by(Trait, Method) |>
  summarise(
    N = n(),
    r = cor(Observed, Predicted),
    RMSE = sqrt(mean((Observed - Predicted)^2)),
    MAE = mean(abs(Observed - Predicted)),
    Bias = mean(Predicted - Observed),
    Slope = cov(Observed, Predicted) / var(Predicted),
    .groups = "drop"
  ) |>
  mutate(
    t_value = r * sqrt((N - 2) / (1 - r^2)),
    p_value = 2 * pt(abs(t_value), df = N - 2, lower.tail = FALSE)
  ) |>
  select(-t_value)

# Resume os resultados no objeto `fold_summary`.
fold_summary <- metrics |>
  group_by(Trait, Method) |>
  summarise(
    Mean_DIC = mean(DIC),
    SD_DIC = sd(DIC),
    Mean_fold_r = mean(Correlation),
    SD_fold_r = sd(Correlation),
    Mean_fold_RMSE = mean(RMSE),
    SD_fold_RMSE = sd(RMSE),
    Mean_fold_MAE = mean(MAE),
    SD_fold_MAE = sd(MAE),
    .groups = "drop"
  )
```

Os p-valores da correlação de Pearson são mantidos como medidas nominais e
descritivas porque os conjuntos de treinamento se sobrepõem entre folds. Eles
são apresentados como contexto e não são usados para definir a elegibilidade
para seleção genômica.

```{r pooled-oof-preview}
pooled_metrics |>
  arrange(match(Trait, traits), desc(r), RMSE) |>
  head(14) |>
  kable(
    digits = 5,
    caption = "Exemplo do desempenho preditivo OOF"
  )
```

## 4. DIC e suporte relativo dos métodos

Seguindo a lógica utilizada no estudo de referência [@silva2021brrguava], a
diferença em relação ao menor DIC médio de cada característica é convertida em
pesos relativos:

\[
\Delta_j=DIC_j-\min(DIC),
\]

\[
Wprob_j=\frac{\exp(-\Delta_j/2)}{\sum_k\exp(-\Delta_k/2)},
\qquad
ER_j=\frac{Wprob_{\max}}{Wprob_j}.
\]

`Wprob` é utilizado aqui como um peso de suporte relativo derivado do DIC.
Ele não é interpretado como uma probabilidade posterior calibrada de um modelo.

```{r model-comparison}
# Integra desempenho por fold e DIC para construir as medidas usadas na comparação dos métodos.
model_comparison <- pooled_metrics |>
  left_join(fold_summary, by = c("Trait", "Method")) |>
  group_by(Trait) |>
  mutate(
    Delta_DIC = Mean_DIC - min(Mean_DIC),
    log_weight = -0.5 * Delta_DIC,
    stable_weight = exp(log_weight - max(log_weight)),
    Wprob = stable_weight / sum(stable_weight),
    # Define os valores de `ER` usados nesta estrutura.
    ER = max(Wprob) / Wprob
  ) |>
  ungroup() |>
  select(
    Trait, Method, Mean_DIC, SD_DIC, Delta_DIC, Wprob, ER,
    N, r, p_value, RMSE, MAE, Bias, Slope,
    Mean_fold_r, SD_fold_r, Mean_fold_RMSE, SD_fold_RMSE,
    Mean_fold_MAE, SD_fold_MAE
  ) |>
  arrange(match(Trait, traits), Delta_DIC)
```

A etapa seguinte organiza um resumo tabular dos objetos e resultados necessários para a interpretação desta seção.

```{r model-comparison-preview}
model_comparison |>
  select(
    Trait, Method, Mean_DIC, Delta_DIC,
    Wprob, ER, r, RMSE, MAE
  ) |>
  head(21) |>
  kable(
    digits = 5,
    caption = "Exemplo da comparação entre os métodos"
  )
```

## 5. Seleção do método por característica

A capacidade preditiva OOF é o critério principal. DIC fornece evidência
complementar sobre ajuste e complexidade, enquanto a variação entre folds e os
diagnósticos MCMC ajudam a avaliar a estabilidade da escolha.

Um método é considerado preditivamente competitivo quando sua média de
correlação entre folds está até um erro-padrão abaixo da maior média e seu RMSE
médio está até um erro-padrão acima do menor RMSE médio. O ajuste por DIC é
considerado semelhante quando `Delta_DIC <= 2`. Esses limites definem um
conjunto competitivo operacional, e não um teste formal de equivalência.

Quando RR-BLUP/BRR pertence a esse conjunto competitivo e não apresenta flags
de convergência, ele é preferido como referência de contração homogênea e de
parametrização mais simples. Caso contrário, é utilizado o melhor candidato
competitivo sem flags; se não houver candidato elegível pela regra, o vencedor
preditivo é mantido como escolha provisória para revisão.

### 5.1 Vencedores preditivo e por DIC

Primeiro são identificados, separadamente, o método com melhor desempenho
preditivo OOF e o método com menor DIC médio em cada característica.

```{r selection-winners}
# Deriva os rankings ou resumos de seleção a partir dos resultados preditivos.
DIC_COMPETITIVE_THRESHOLD <- 2
N_FOLDS <- matrices_object$settings$n_folds

# Melhor combinação de correlação, RMSE, MAE e DIC para cada característica.
predictive_winners <- model_comparison |>
  group_by(Trait) |>
  arrange(desc(r), RMSE, MAE, Mean_DIC, .by_group = TRUE) |>
  slice(1L) |>
  ungroup() |>
  transmute(
    Trait,
    Predictive_winner = Method,
    Predictive_winner_r = r,
    Predictive_winner_p_value = p_value,
    Predictive_winner_RMSE = RMSE,
    Predictive_winner_MAE = MAE
  )

# Método com o menor DIC médio para cada característica.
dic_winners <- model_comparison |>
  group_by(Trait) |>
  arrange(Mean_DIC, desc(r), RMSE, MAE, .by_group = TRUE) |>
  slice(1L) |>
  ungroup() |>
  transmute(
    Trait,
    DIC_winner = Method,
    Minimum_DIC = Mean_DIC,
    DIC_winner_Wprob = Wprob
  )
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selection-winners-table}
predictive_winners |>
  left_join(dic_winners, by = "Trait") |>
  select(
    Trait, Predictive_winner, DIC_winner,
    Predictive_winner_r, Predictive_winner_RMSE,
    Minimum_DIC
  ) |>
  kable(
    digits = 5,
    caption = "Vencedores preditivo e por DIC em cada característica"
  )
```

### 5.2 Diagnósticos MCMC por característica e método

Os diagnósticos das cadeias são resumidos antes de definir o conjunto de
métodos elegíveis para escolha.

```{r selection-convergence-summary}
convergence_by_trait_method <- convergence |>
  group_by(Trait, Method) |>
  summarise(
    Runs = n_distinct(Run_id),
    Parameters_reviewed = n(),
    Parameters_flagged = sum(Review_flag),
    Runs_with_flags = n_distinct(Run_id[Review_flag]),
    Minimum_ESS = min(Effective_sample_size, na.rm = TRUE),
    Maximum_absolute_Geweke = max(abs(Geweke_Z), na.rm = TRUE),
    .groups = "drop"
  )
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selection-convergence-table}
convergence_by_trait_method |>
  arrange(match(Trait, traits), match(Method, methods)) |>
  head(21) |>
  kable(
    digits = 4,
    caption = "Exemplo dos diagnósticos MCMC resumidos por característica e método"
  )
```

### 5.3 Conjunto competitivo

A regra de um erro-padrão é aplicada à correlação e ao RMSE médios entre folds.
Em seguida, o limite `Delta_DIC <= 2` e os diagnósticos MCMC definem os
candidatos elegíveis pela regra. Como os conjuntos de treinamento se sobrepõem
entre folds, esses erros-padrão são tolerâncias descritivas e não são
interpretados como erros-padrão inferenciais de amostras independentes.

```{r selection-competitive-set}
# Deriva os rankings ou resumos de seleção a partir dos resultados preditivos.
model_selection_support <- model_comparison |>
  left_join(
    convergence_by_trait_method,
    by = c("Trait", "Method")
  ) |>
  left_join(
    predictive_winners |>
      select(Trait, Predictive_winner),
    by = "Trait"
  ) |>
  group_by(Trait) |>
  mutate(
    # Referências para a regra de um erro-padrão.
    Best_mean_fold_r = max(Mean_fold_r, na.rm = TRUE),
    Best_mean_fold_r_SE =
      SD_fold_r[which.max(Mean_fold_r)] / sqrt(N_FOLDS),
    Best_mean_fold_RMSE = min(Mean_fold_RMSE, na.rm = TRUE),
    Best_mean_fold_RMSE_SE =
      SD_fold_RMSE[which.min(Mean_fold_RMSE)] / sqrt(N_FOLDS),

    # Competitividade preditiva.
    Within_one_SE_r =
      Mean_fold_r >= Best_mean_fold_r - Best_mean_fold_r_SE,
    Within_one_SE_RMSE =
      Mean_fold_RMSE <= Best_mean_fold_RMSE + Best_mean_fold_RMSE_SE,
    Predictively_competitive =
      Within_one_SE_r & Within_one_SE_RMSE,

    # Suporte complementar por DIC e convergência.
    DIC_competitive_threshold = DIC_COMPETITIVE_THRESHOLD,
    DIC_competitive = Delta_DIC <= DIC_COMPETITIVE_THRESHOLD,
    Competitive_for_selection =
      Predictively_competitive & DIC_competitive,
    Convergence_review = Parameters_flagged > 0,
    Automatic_candidate =
      Competitive_for_selection & !Convergence_review,

    # Resumo do conjunto de candidatos em cada característica.
    Competitive_set_n = sum(Competitive_for_selection),
    Automatic_candidate_n = sum(Automatic_candidate),
    RRBLUP_competitive = any(
      Method == "RRBLUP" & Competitive_for_selection
    ),
    RRBLUP_automatic_candidate = any(
      Method == "RRBLUP" & Automatic_candidate
    ),
    Automatic_selection_available = any(Automatic_candidate),

    # RR-BLUP recebe prioridade apenas dentro do conjunto automático.
    Selection_priority = case_when(
      Automatic_candidate & Method == "RRBLUP" ~ 1L,
      Automatic_candidate ~ 2L,
      TRUE ~ 3L
    )
  ) |>
  ungroup() |>
  arrange(
    match(Trait, traits),
    Selection_priority,
    desc(r),
    RMSE,
    MAE,
    Mean_DIC
  )
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selection-competitive-table}
model_selection_support |>
  select(
    Trait, Method, Predictively_competitive,
    DIC_competitive, Convergence_review,
    Automatic_candidate, r, RMSE, Delta_DIC
  ) |>
  head(21) |>
  kable(
    digits = 5,
    caption = "Exemplo da definição do conjunto competitivo"
  )
```

### 5.4 Escolha do método

Entre os candidatos automáticos, RR-BLUP recebe prioridade. Na ausência de
candidato automático, o vencedor preditivo é mantido como escolha provisória.

```{r selection-choice}
# Melhor candidato automático em cada característica.
automatic_selections <- model_selection_support |>
  filter(Automatic_candidate) |>
  group_by(Trait) |>
  arrange(
    Selection_priority,
    desc(r),
    RMSE,
    MAE,
    Mean_DIC,
    .by_group = TRUE
  ) |>
  slice(1L) |>
  ungroup() |>
  transmute(
    Trait,
    Automatic_selected_method = Method
  )

# Escolha final antes de anexar as métricas do método selecionado.
# Deriva a estrutura usada para priorizar ou resumir os candidatos.
selection_spec <- predictive_winners |>
  select(Trait, Predictive_winner) |>
  left_join(automatic_selections, by = "Trait") |>
  mutate(
    Selected_method = coalesce(
      Automatic_selected_method,
      Predictive_winner
    )
  ) |>
  select(Trait, Selected_method, Predictive_winner) |>
  left_join(
    model_selection_support |>
      # Mantém e organiza as variáveis usadas no resultado.
      select(
        Trait,
        Method,
        Automatic_selection_available,
        Automatic_candidate_n,
        Competitive_set_n,
        RRBLUP_competitive,
        RRBLUP_automatic_candidate,
        Convergence_review
      ),
    # Registra os valores de `by` que completam esta estrutura.
    by = c("Trait", "Selected_method" = "Method")
  ) |>
  mutate(
    RRBLUP_preference_applied =
      Selected_method == "RRBLUP" &
      Predictive_winner != "RRBLUP" &
      RRBLUP_automatic_candidate,
    Selection_requires_review =
      !Automatic_selection_available | Convergence_review,
    Selection_status = case_when(
      Selection_requires_review ~ "provisional_review_required",
      TRUE ~ "rule_based_selected"
    ),
    Decision_code = case_when(
      Selection_requires_review ~
        "predictive_winner_provisional_review_required",
      Selected_method == "RRBLUP" &
        RRBLUP_preference_applied ~
        "rrblup_preferred_among_competitive_models",
      Selected_method == "RRBLUP" &
        Predictive_winner == "RRBLUP" ~
        "rrblup_best_predictive_model",
      TRUE ~ "best_non_rrblup_competitive_model"
    ),
    Operational_competitive_set_rule =
      "one_se_r_and_rmse_plus_delta_dic_le_2"
  ) |>
  select(-Predictive_winner, -Convergence_review) |>
  arrange(match(Trait, traits))
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selection-choice-table}
selection_spec |>
  select(
    Trait, Selected_method, Selection_status,
    RRBLUP_preference_applied,
    Competitive_set_n, Automatic_candidate_n
  ) |>
  kable(caption = "Método escolhido após a aplicação da regra hierárquica")
```

### 5.5 Desempenho dos métodos selecionados

As métricas e os diagnósticos do método efetivamente selecionado são então
extraídos da tabela de suporte.

```{r selected-performance}
# Extrai métricas preditivas, DIC e diagnósticos do método selecionado em cada característica.
selected_performance <- model_selection_support |>
  inner_join(
    selection_spec |>
      select(Trait, Selected_method),
    by = "Trait"
  ) |>
  # Restringe os dados às observações relevantes nesta etapa.
  filter(Method == Selected_method) |>
  # Constrói as colunas que formarão a saída deste bloco.
  transmute(
    Trait,
    Selected_method,
    r,
    p_value,
    RMSE,
    MAE,
    Bias,
    Slope,
    Mean_DIC,
    Delta_DIC,
    Wprob,
    ER,
    Mean_fold_r,
    SD_fold_r,
    Mean_fold_RMSE,
    SD_fold_RMSE,
    Within_one_SE_r,
    Within_one_SE_RMSE,
    Predictively_competitive,
    DIC_competitive,
    Competitive_for_selection,
    Automatic_candidate,
    Competitive_set_n,
    Automatic_candidate_n,
    RRBLUP_competitive,
    RRBLUP_automatic_candidate,
    Parameters_flagged,
    Runs_with_flags,
    Minimum_ESS,
    Maximum_absolute_Geweke,
    Convergence_review
  )
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selected-performance-table}
selected_performance |>
  select(
    Trait, Selected_method, r, p_value, RMSE,
    Mean_DIC, Delta_DIC, Parameters_flagged
  ) |>
  kable(
    digits = 5,
    caption = "Desempenho e diagnóstico dos métodos selecionados"
  )
```

### 5.6 Resultado final da seleção e evidência preditiva

Por fim, são reunidos o método selecionado, os vencedores preditivo e por DIC, a
direção OOF e uma classe nominal do sinal de cada característica. A classe
nominal é apresentada apenas para tornar explícitas correlações positivas
fracas; ela não é usada como filtro de seleção.

```{r final-model-selection}
# Deriva os rankings ou resumos de seleção a partir dos resultados preditivos.
model_selection <- selection_spec |>
  left_join(
    selected_performance,
    # Registra os valores de `by` que completam esta estrutura.
    by = c("Trait", "Selected_method")
  ) |>
  left_join(predictive_winners, by = "Trait") |>
  left_join(dic_winners, by = "Trait") |>
  mutate(
    Predictive_loss = Predictive_winner_r - r,
    Predictive_winner_agrees =
      Selected_method == Predictive_winner,
    DIC_winner_agrees =
      Selected_method == DIC_winner,
    OOF_predictive_direction = if_else(
      r > 0,
      "positive",
      "nonpositive"
    ),
    OOF_nominal_signal = case_when(
      r > 0 & p_value < 0.05 ~ "positive_nominal_p_lt_0.05",
      r > 0 ~ "positive_nominal_p_ge_0.05",
      TRUE ~ "nonpositive"
    )
  ) |>
  arrange(match(Trait, traits))

# Define o caminho ou local associado a `oof_direction_summary`.
oof_direction_summary <- model_selection |>
  count(OOF_predictive_direction, name = "Traits") |>
  arrange(OOF_predictive_direction)
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r model-selection-table}
model_selection |>
  select(
    Trait, Selected_method, Predictive_winner, DIC_winner,
    r, p_value, RMSE, Mean_DIC, Delta_DIC,
    RRBLUP_preference_applied,
    Selection_status, OOF_predictive_direction, OOF_nominal_signal
  ) |>
  kable(
    digits = 5,
    caption = "Método selecionado por característica"
  )
```

A etapa seguinte organiza um resumo tabular dos objetos e resultados necessários para a interpretação desta seção.

```{r oof-direction-table}
kable(
  oof_direction_summary,
  caption = "Direção preditiva OOF das características"
)
```

## 6. Estabilidade entre folds dos métodos selecionados

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selected-fold-diagnostics}
selected_fold_diagnostics <- metrics |>
  inner_join(
    selection_spec |> select(Trait, Selected_method),
    by = c("Trait" = "Trait", "Method" = "Selected_method")
  ) |>
  transmute(Trait, Fold, Method, Correlation, RMSE, MAE) |>
  arrange(match(Trait, traits), Fold)

selected_fold_diagnostics |>
  head(15) |>
  kable(
    digits = 5,
    caption = "Exemplo do desempenho por fold dos métodos selecionados"
  )
```

## 7. Tempo computacional

A etapa seguinte resume os tempos de execução para comparar o custo computacional dos métodos.

```{r runtime}
# Resume o custo computacional dos métodos avaliados.
runtime_summary <- metrics |>
  group_by(Method) |>
  summarise(
    Fits = n(),
    Total_seconds = sum(Elapsed_seconds),
    Mean_seconds = mean(Elapsed_seconds),
    SD_seconds = sd(Elapsed_seconds),
    Median_seconds = median(Elapsed_seconds),
    Minimum_seconds = min(Elapsed_seconds),
    Maximum_seconds = max(Elapsed_seconds),
    .groups = "drop"
  ) |>
  arrange(match(Method, methods))

# Formata a tabela para exibição no tutorial.
kable(
  runtime_summary,
  digits = 3,
  caption = "Tempo computacional dos sete métodos"
)
```

## 8. Análogo de herdabilidade baseado em marcadores e acurácia ajustada no RR-BLUP/BRR

Seguindo Silva et al. [@silva2021brrguava], um análogo de variância aditiva
baseado em marcadores é obtido a partir da variância comum dos marcadores e do
denominador \(\sum 2pq\). A acurácia ajustada por fold é definida como
\(r_f/\sqrt{H_f^2}\). O \(H^2\) resultante é apresentado como um análogo de
herdabilidade baseado no modelo, e não como uma estimativa clássica independente
de herdabilidade no sentido restrito.

```{r rrblup-heritability}
# Calcula e organiza o resumo de herdabilidade baseado no modelo RR-BLUP/BRR.
rrblup_variance_components <- convergence |>
  filter(
    Method == "RRBLUP",
    Parameter %in% c("Residual_variance", "Marker_variance")
  ) |>
  select(Run_id, Trait, Fold, Method, Parameter, Posterior_mean) |>
  pivot_wider(names_from = Parameter, values_from = Posterior_mean)

rrblup_fold_correlations <- metrics |>
  filter(Method == "RRBLUP") |>
  select(Trait, Fold, Correlation)

# Organiza a estrutura da validação cruzada e dos ajustes.
rrblup_heritability_by_fold <- rrblup_variance_components |>
  left_join(rrblup_fold_correlations, by = c("Trait", "Fold")) |>
  mutate(
    Marker_variance_denominator = matrices_object$denominator,
    Additive_variance = Marker_variance * Marker_variance_denominator,
    H2 = Additive_variance / (Additive_variance + Residual_variance),
    Predictive_accuracy = Correlation / sqrt(H2)
  ) |>
  arrange(match(Trait, traits), Fold)

# Resume os resultados no objeto `rrblup_heritability_summary`.
rrblup_heritability_summary <- rrblup_heritability_by_fold |>
  group_by(Trait) |>
  summarise(
    Mean_H2 = mean(H2),
    SD_H2 = sd(H2),
    Mean_fold_r = mean(Correlation),
    SD_fold_r = sd(Correlation),
    Mean_predictive_accuracy = mean(Predictive_accuracy),
    SD_predictive_accuracy = sd(Predictive_accuracy),
    .groups = "drop"
  ) |>
  arrange(match(Trait, traits))

kable(
  rrblup_heritability_summary,
  digits = 5,
  caption = "Análogo de herdabilidade baseado em marcadores e acurácia preditiva ajustada no RR-BLUP/BRR"
)
```

## 9. Valores genômicos OOF e direção de seleção

O valor genômico de cada indivíduo é cross-fitted: ele provém do fold em que o
fenótipo desse indivíduo foi mascarado. Esses valores genômicos OOF permitem um
ranking coerente com a validação, pois o próprio fenótipo do indivíduo não entra
no seu escore. Eles não correspondem a valores genéticos finais de um modelo
reajustado com os 150 fenótipos.

A direção de seleção é aplicada ao valor genômico somente no momento de
construir o ranking. O valor genômico original permanece inalterado.

```{r selected-oof-genomic-values}
# Constrói ou verifica as matrizes usadas na predição genômica.
selection_objective_map <- c(
  MMCOM = "increase",
  PRODCOM = "increase",
  PRODEF = "decrease",
  CF = "increase",
  DF = "increase",
  DCI = "decrease",
  EP = "increase",
  FFC = "increase",
  FFP = "increase",
  SS = "increase"
)

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
selection_objectives <- data.frame(
  Trait = traits,
  Selection_objective = unname(selection_objective_map[traits]),
  stringsAsFactors = FALSE
) |>
  mutate(
    Ranking_rule = case_when(
      Selection_objective == "increase" ~ "maximize_genomic_value",
      Selection_objective == "decrease" ~ "minimize_genomic_value",
      TRUE ~ NA_character_
    )
  )

# Organiza o componente genômico estimado para cada indivíduo.
selected_oof_genomic_values <- predictions |>
  inner_join(
    model_selection |>
      select(
        Trait, Selected_method, OOF_predictive_direction,
        OOF_nominal_signal, r, p_value
      ),
    by = "Trait"
  ) |>
  inner_join(selection_objectives, by = "Trait") |>
  filter(Method == Selected_method) |>
  transmute(
    IND, Trait, Fold, Method, OOF_predictive_direction,
    OOF_nominal_signal, OOF_r = r, OOF_nominal_p_value = p_value,
    Selection_objective, Ranking_rule,
    Genomic_value = Genomic_component
  ) |>
  arrange(match(Trait, traits), IND)

# Formata a tabela para exibição no tutorial.
kable(
  selection_objectives,
  caption = "Direção de seleção definida para cada característica"
)
```

## 10. Ranking exploratório proporcional de candidatos por característica

Os rankings de candidatos são apresentados para as características cujo método
selecionado possui correlação OOF positiva. Os p-valores nominais permanecem
descritivos e não determinam se um ranking é produzido. Correlações positivas
com `p >= 0,05` são mantidas explicitamente como sinais fracos/exploratórios e
não devem ser interpretadas como utilidade preditiva demonstrada. Em cada
característica com OOF positivo, são listados os 20% de indivíduos com maior
escore orientado; essa proporção é uma regra configurável do tutorial, e não
uma intensidade ótima estimada. Para características cujo objetivo é diminuir,
o escore é o negativo do valor genômico.

```{r genomic-ranking}
# Constrói ou verifica as matrizes usadas na predição genômica.
genomic_ranking <- selected_oof_genomic_values |>
  mutate(
    Selection_score = case_when(
      Selection_objective == "increase" ~ Genomic_value,
      Selection_objective == "decrease" ~ -Genomic_value,
      TRUE ~ NA_real_
    )
  ) |>
  group_by(Trait) |>
  arrange(desc(Selection_score), IND, .by_group = TRUE) |>
  mutate(Rank = row_number()) |>
  ungroup() |>
  arrange(match(Trait, traits), Rank)

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
SELECTION_PROPORTION <- 0.20

positive_oof_traits <- model_selection |>
  filter(OOF_predictive_direction == "positive") |>
  pull(Trait)

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
selection_sizes <- genomic_ranking |>
  filter(Trait %in% positive_oof_traits) |>
  count(Trait, name = "N_available") |>
  mutate(
    Selection_proportion = SELECTION_PROPORTION,
    N_selected = pmax(
      1L,
      as.integer(ceiling(SELECTION_PROPORTION * N_available))
    )
  ) |>
  arrange(match(Trait, traits))

# Resume os resultados no objeto `selection_cutoff_summary`.
selection_cutoff_summary <- genomic_ranking |>
  filter(Trait %in% positive_oof_traits) |>
  inner_join(
    selection_sizes |>
      select(Trait, N_available, N_selected),
    by = "Trait"
  ) |>
  arrange(match(Trait, traits), Rank) |>
  group_by(Trait) |>
  mutate(Next_score = lead(Selection_score)) |>
  summarise(
    N_available = first(N_available),
    N_selected = first(N_selected),
    Score_last_selected = Selection_score[first(N_selected)],
    Score_first_not_selected = Next_score[first(N_selected)],
    Exact_boundary_tie =
      !is.na(Score_first_not_selected) &
      Score_last_selected == Score_first_not_selected,
    .groups = "drop"
  ) |>
  arrange(match(Trait, traits))

# Mantém, em cada característica com OOF positivo, os candidatos incluídos na proporção definida para o ranking.
selected_individuals_by_trait <- genomic_ranking |>
  inner_join(selection_sizes, by = "Trait") |>
  filter(Rank <= N_selected) |>
  left_join(
    matrices_object$metadata |>
      select(IND, FAM),
    by = "IND"
  ) |>
  select(
    Trait, Rank, IND, FAM, Method,
    Selection_objective, Ranking_rule,
    Genomic_value, Selection_score, OOF_predictive_direction,
    OOF_nominal_signal, OOF_r, OOF_nominal_p_value,
    Selection_proportion, N_available, N_selected
  ) |>
  arrange(match(Trait, traits), Rank)
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selection-size-table}
kable(
  selection_sizes,
  digits = 2,
  caption = "Tamanho do ranking exploratório de candidatos por característica com OOF positivo"
)
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selection-cutoff-table}
kable(
  selection_cutoff_summary,
  digits = 5,
  caption = "Escores no limite da seleção proporcional"
)
```

A etapa seguinte deriva e apresenta a regra de seleção ou priorização correspondente a partir dos resultados já calculados.

```{r selected-individuals-preview}
selected_individuals_by_trait |>
  group_by(Trait) |>
  slice_head(n = 5) |>
  ungroup() |>
  kable(
    digits = 5,
    caption = "Cinco primeiros candidatos do ranking em cada característica com OOF positivo"
  )
```

## 11. Recorrência entre características

A recorrência é descritiva: entre as características com correlação OOF positiva,
conta em quantas cada indivíduo aparece entre os 20% mais favoráveis. Sinais OOF
positivos fracos permanecem exploratórios. Não são usados pesos e não é
construído índice de seleção multicaracterística.

```{r multitrait-recurrence}
# Conta a recorrência dos indivíduos entre os rankings exploratórios das características com OOF positivo.
recurrence_counts <- selected_individuals_by_trait |>
  arrange(match(Trait, traits), Rank) |>
  group_by(IND, FAM) |>
  summarise(
    N_traits = n(),
    Traits = paste(Trait, collapse = ", "),
    Best_rank = min(Rank),
    Mean_rank_present = mean(Rank),
    .groups = "drop"
  )

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
multitrait_recurrence <- matrices_object$metadata |>
  select(IND, FAM) |>
  left_join(recurrence_counts, by = c("IND", "FAM")) |>
  mutate(
    N_traits = tidyr::replace_na(N_traits, 0L),
    Traits = tidyr::replace_na(Traits, ""),
    Best_rank = case_when(
      N_traits == 0L ~ NA_integer_,
      TRUE ~ Best_rank
    ),
    Mean_rank_present = case_when(
      N_traits == 0L ~ NA_real_,
      TRUE ~ Mean_rank_present
    ),
    Selection_frequency = N_traits / length(positive_oof_traits),
    Recurrence_class = case_when(
      N_traits >= 4L ~ "high_4plus",
      N_traits == 3L ~ "intermediate_3",
      N_traits == 2L ~ "broad_2",
      N_traits == 1L ~ "specific_1",
      TRUE ~ "none_0"
    )
  ) |>
  arrange(desc(N_traits), FAM, IND)

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
recurrence_distribution <- multitrait_recurrence |>
  count(N_traits, Recurrence_class, name = "N_individuals") |>
  mutate(
    Proportion_of_population =
      N_individuals / nrow(matrices_object$metadata)
  ) |>
  arrange(desc(N_traits))

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
candidate_rank_wide <- selected_individuals_by_trait |>
  semi_join(
    multitrait_recurrence |>
      filter(N_traits >= 2L),
    by = c("IND", "FAM")
  ) |>
  select(IND, FAM, Trait, Rank) |>
  pivot_wider(
    names_from = Trait,
    values_from = Rank,
    names_glue = "{Trait}_rank"
  )

# Deriva a estrutura usada para priorizar ou resumir os candidatos.
multitrait_candidates <- multitrait_recurrence |>
  filter(N_traits >= 2L) |>
  left_join(candidate_rank_wide, by = c("IND", "FAM")) |>
  arrange(desc(N_traits), FAM, IND)

kable(
  recurrence_distribution,
  digits = 4,
  caption = "Distribuição da recorrência entre características com correlação OOF positiva"
)
```

## 12. Correlações entre valores genômicos OOF

Estas correlações descrevem a associação entre valores genômicos preditos e não
são estimativas de correlação genética obtidas de um modelo multivariado.

```{r oof-correlations}
selected_genomic_wide <- selected_oof_genomic_values |>
  select(IND, Trait, Genomic_value) |>
  pivot_wider(names_from = Trait, values_from = Genomic_value)

selected_oof_correlations <- cor(
  as.matrix(selected_genomic_wide[, traits, drop = FALSE])
)

kable(
  round(selected_oof_correlations, 3),
  caption = "Correlações entre valores genômicos OOF"
)
```

## 13. Gravação dos resultados

A etapa seguinte organiza e salva as saídas produzidas para uso nos módulos posteriores.

```{r save-results}
# Agrupa configurações, resultados e diagnósticos no objeto final do módulo.
write.csv(model_comparison, file.path(RESULTS_DIR, "model_comparison.csv"), row.names = FALSE)
write.csv(model_selection_support, file.path(RESULTS_DIR, "model_selection_support.csv"), row.names = FALSE)
write.csv(model_selection, file.path(RESULTS_DIR, "model_selection.csv"), row.names = FALSE)
write.csv(selection_spec, file.path(RESULTS_DIR, "selection_decision_spec.csv"), row.names = FALSE)
write.csv(oof_direction_summary, file.path(RESULTS_DIR, "oof_direction_summary.csv"), row.names = FALSE)
write.csv(convergence_by_trait_method, file.path(RESULTS_DIR, "convergence_by_trait_method.csv"), row.names = FALSE)
write.csv(selected_fold_diagnostics, file.path(RESULTS_DIR, "selected_fold_diagnostics.csv"), row.names = FALSE)
write.csv(runtime_summary, file.path(RESULTS_DIR, "runtime_summary.csv"), row.names = FALSE)
write.csv(rrblup_heritability_by_fold, file.path(RESULTS_DIR, "rrblup_heritability_by_fold.csv"), row.names = FALSE)
write.csv(rrblup_heritability_summary, file.path(RESULTS_DIR, "rrblup_heritability_summary.csv"), row.names = FALSE)
write.csv(selection_objectives, file.path(RESULTS_DIR, "selection_objectives.csv"), row.names = FALSE)
write.csv(selected_oof_genomic_values, file.path(RESULTS_DIR, "selected_oof_genomic_values.csv"), row.names = FALSE)
# Salva a saída consolidada para uso posterior.
write.csv(genomic_ranking, file.path(RESULTS_DIR, "genomic_ranking.csv"), row.names = FALSE)
write.csv(selection_sizes, file.path(RESULTS_DIR, "selection_sizes.csv"), row.names = FALSE)
write.csv(selection_cutoff_summary, file.path(RESULTS_DIR, "selection_cutoff_summary.csv"), row.names = FALSE)
write.csv(selected_individuals_by_trait, file.path(RESULTS_DIR, "selected_individuals_by_trait.csv"), row.names = FALSE)
write.csv(multitrait_recurrence, file.path(RESULTS_DIR, "multitrait_recurrence.csv"), row.names = FALSE)
write.csv(recurrence_distribution, file.path(RESULTS_DIR, "recurrence_distribution.csv"), row.names = FALSE)
write.csv(multitrait_candidates, file.path(RESULTS_DIR, "multitrait_candidates.csv"), row.names = FALSE)
# Salva a saída consolidada para uso posterior.
write.csv(as.data.frame(selected_oof_correlations), file.path(RESULTS_DIR, "selected_oof_correlations.csv"), row.names = TRUE)

# Salva a saída consolidada para uso posterior.
saveRDS(
  list(
    traits = traits,
    methods = methods,
    model_comparison = model_comparison,
    model_selection_support = model_selection_support,
    selection_spec = selection_spec,
    model_selection = model_selection,
    oof_direction_summary = oof_direction_summary,
    convergence_by_trait_method = convergence_by_trait_method,
    selected_fold_diagnostics = selected_fold_diagnostics,
    runtime = runtime_summary,
    rrblup_heritability_by_fold = rrblup_heritability_by_fold,
    rrblup_heritability_summary = rrblup_heritability_summary,
    selection_objectives = selection_objectives,
    selected_oof_genomic_values = selected_oof_genomic_values,
    genomic_ranking = genomic_ranking,
    selection_proportion = SELECTION_PROPORTION,
    positive_oof_traits = positive_oof_traits,
    selection_sizes = selection_sizes,
    selection_cutoff_summary = selection_cutoff_summary,
    selected_individuals_by_trait = selected_individuals_by_trait,
    multitrait_recurrence = multitrait_recurrence,
    recurrence_distribution = recurrence_distribution,
    multitrait_candidates = multitrait_candidates,
    selected_oof_correlations = selected_oof_correlations
  ),
  file.path(RESULTS_DIR, "module04_results.rds")
)
```

## 14. Próxima etapa

O Módulo 05 utiliza esses resultados para construir as tabelas e figuras de
síntese da comparação dos métodos e da seleção genômica.
